#include<stdio.h>

void main()
{
    // char str[100];
    //  int len;
    // printf("Enter the the string:\n");
    // scanf("%s",&str);

    // len = strlen(str);
    // printf(" length of string is %d",len);

    //strcat
    char name1[]="am";
    char name2[]="eet";
  
    strcat(name1,name2);
  printf("The concatenate string is %s",name1                                                                                                                                                                                                                   );
       
}